// c8_stdin_stdout_repeticao.dart
// 🎯 Treino repetitivo: stdout.write e stdin.readLineSync
// Foco total em entrada e saída de dados

import 'dart:io';
import 'package:chalkdart/chalk.dart';

void main() {
  print(chalk.blue('💙 Treino: stdout e stdin'));
  print(chalk.yellow('----------------------------------\n'));

  // // 1️⃣
  // stdout.write(chalk.yellow('1️⃣  Digite algo: '));
  // final entrada1 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada1\n'));
  stdout.write(chalk.yellow('1 - Digite algo: '));
  final entrada1 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada1 \n'));

  // 2️⃣
  // stdout.write(chalk.yellow('2️⃣  Digite algo: '));
  // final entrada2 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada2\n'));
  stdout.write(chalk.yellow('2 - Digite algo: '));
  final entrada2 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada2 \n'));

  // 3️⃣
  // stdout.write(chalk.yellow('3️⃣  Digite algo: '));
  // final entrada3 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada3\n'));

  stdout.write(chalk.yellow('3 Digite algo: '));
  final entrada3 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada3 \n'));

  // // 4️⃣
  // stdout.write(chalk.yellow('4️⃣  Digite algo: '));
  // final entrada4 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada4\n'));
  stdout.write(chalk.yellow('4 - Digite algo: '));
  final entrada4 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada4 \n'));

  // // 5️⃣
  // stdout.write(chalk.yellow('5️⃣  Digite algo: '));
  // final entrada5 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada5\n'));

  stdout.write(chalk.yellow('5 - Digite algo: '));
  final entrada5 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada5 \n'));
  // // 6️⃣
  // stdout.write(chalk.yellow('6️⃣  Digite algo: '));
  // final entrada6 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada6\n'));
  stdout.write(chalk.yellow('6 - Digite algo: '));
  final entrada6 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada6 \n'));

  // // 7️⃣
  // stdout.write(chalk.yellow('7️⃣  Digite algo: '));
  // final entrada7 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada7\n'));
  stdout.write(chalk.yellow('7 - Digite algo: '));
  final entrada7 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada7 \n'));

  // // 8️⃣
  // stdout.write(chalk.yellow('8️⃣  Digite algo: '));
  // final entrada8 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada8\n'));
  stdout.write(chalk.yellow('8 Digite algo: '));
  final entrada8 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada8 \n'));

  // // 9️⃣
  // stdout.write(chalk.yellow('9️⃣  Digite algo: '));
  // final entrada9 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada9\n'));
  stdout.write(chalk.yellow('9 - Digite algo: '));
  final entrada9 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada9 \n'));

  // // 🔟
  // stdout.write(chalk.yellow('🔟  Digite algo: '));
  // final entrada10 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada10\n'));
  stdout.write(chalk.yellow('10 - Digite algo: '));
  final entrada10 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada10 \n'));
  // // 🔁 continua até 20 só pra prática
  // stdout.write(chalk.yellow('11️⃣  Digite algo: '));
  // final entrada11 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada11\n'));

  stdout.write(chalk.yellow('11 - Digite algo: '));
  final entrada11 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada11 \n'));

  // stdout.write(chalk.yellow('12️⃣  Digite algo: '));
  // final entrada12 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada12\n'));
  stdout.write(chalk.yellow('12 - Digite algo: '));
  final entrada12 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada12\n'));

  // stdout.write(chalk.yellow('13️⃣  Digite algo: '));
  // final entrada13 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada13\n'));
  stdout.write(chalk.yellow('13 Digite algo: '));
  final entrada13 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada13 \n'));

  // stdout.write(chalk.yellow('14️⃣  Digite algo: '));
  // final entrada14 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada14\n'));
  stdout.write(chalk.yellow('14 - Digite algo: '));
  final entrada14 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada14 \n'));

  // stdout.write(chalk.yellow('15️⃣  Digite algo: '));
  // final entrada15 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada15\n'));
  stdout.write(chalk.yellow('15 - Digite algo: '));
  final entrada15 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada15 \n'));

  // stdout.write(chalk.yellow('16️⃣  Digite algo: '));
  // final entrada16 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada16\n'));
  stdout.write(chalk.yellow('16 - Digite algo: '));
  final entrada16 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada16 \n'));

  // stdout.write(chalk.yellow('17️⃣  Digite algo: '));
  // final entrada17 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada17\n'));
  stdout.write(chalk.yellow('17 Digite algo: '));
  final entrada17 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada17 \n'));

  // stdout.write(chalk.yellow('18️⃣  Digite algo: '));
  // final entrada18 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada18\n'));
  stdout.write(chalk.yellow('18 Digite algo: '));
  final entrada18 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada18 \n'));
  // stdout.write(chalk.yellow('19️⃣  Digite algo: '));
  // final entrada19 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada19\n'));
  stdout.write(chalk.yellow('19 Digite algo: '));
  final entrada19 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada19 \n'));

  // stdout.write(chalk.yellow('2️⃣0️⃣  Digite algo: '));
  // final entrada20 = stdin.readLineSync();
  // print(chalk.green('Você digitou: $entrada20\n'));

  stdout.write(chalk.yellow('20 Digite algo:'));
  final entrada20 = stdin.readLineSync();
  print(chalk.green('Você digitou: $entrada20 \n'));

  print(chalk.blue('💙 Fim do treino de stdout/stdin.'));
}
